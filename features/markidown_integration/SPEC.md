If markitdown is installed use it to transform pdf word etc to md files before displaying them. In memory. Also allow other file types than only .md
for now we should allow pictures word files, pdf, excel files and jpg and png

